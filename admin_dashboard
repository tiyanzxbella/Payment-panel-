<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin Dashboard - Yola Assistant</title>
    <style>
        body {
            font-family: 'Arial', sans-serif;
            background-image: url('https://files.catbox.moe/ele9lp.jpg'); /* Your background image */
            background-size: cover;
            background-position: center;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
            margin: 0;
            color: #333;
            text-align: center;
        }

        .dashboard-container {
            background-color: rgba(255, 255, 255, 0.95);
            padding: 40px;
            border-radius: 15px;
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.3);
            width: 100%;
            max-width: 600px;
            box-sizing: border-box;
            animation: fadeIn 0.8s ease-out;
        }

        @keyframes fadeIn {
            from { opacity: 0; transform: translateY(-20px); }
            to { opacity: 1; transform: translateY(0); }
        }

        h1 {
            color: #7873f5;
            margin-bottom: 20px;
            font-size: 32px;
            text-shadow: 1px 1px 2px rgba(0,0,0,0.1);
        }

        p {
            font-size: 18px;
            line-height: 1.6;
            margin-bottom: 30px;
            color: #555;
        }

        .logout-button {
            background: linear-gradient(45deg, #e74c3c, #c0392b);
            color: white;
            padding: 12px 25px;
            border: none;
            border-radius: 8px;
            font-size: 16px;
            font-weight: bold;
            cursor: pointer;
            transition: transform 0.3s ease, box-shadow 0.3s ease;
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.2);
            text-decoration: none; /* For anchor tag */
            display: inline-block; /* For anchor tag */
        }

        .logout-button:hover {
            transform: translateY(-3px);
            box-shadow: 0 6px 15px rgba(0, 0, 0, 0.3);
        }

        @media (max-width: 600px) {
            .dashboard-container {
                padding: 30px 20px;
                margin: 20px;
            }
            h1 {
                font-size: 28px;
            }
            p {
                font-size: 16px;
            }
            .logout-button {
                font-size: 14px;
                padding: 10px 20px;
            }
        }
    </style>
</head>
<body>
    <div class="dashboard-container">
        <h1>Selamat Datang, Admin Yola!</h1>
        <p>Ini adalah halaman dashboard admin. Di sini Anda dapat menambahkan fitur untuk mengelola web chat, melihat statistik, atau mengubah pengaturan.</p>
        <p>Untuk fungsionalitas manajemen web chat yang sebenarnya (seperti mengubah respons AI, melihat riwayat chat), Anda perlu mengimplementasikan `backend server` dan `API` yang terhubung ke database.</p>
        <a href="admin_login.html" class="logout-button">Logout</a>
    </div>
</body>
</html>
